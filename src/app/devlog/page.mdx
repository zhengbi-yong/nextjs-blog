import Link from "next/link";
import Button from "@mui/material/Button";

# Developer Log

This is some **bold** and _italics_ text.

This is a list in markdown:

- One
- Two
- Three

$$
  e=mc^2
$$

$$
x=a
$$

<Link href="/">Home</Link>
<Button href="/" variant="contained">
  Home
</Button>

```python
import torch
print("hello world!")
```

Custom React component:

如果需要使用额外的组件，则需要在项目根目录下的mdx-components.tsx中增加对相应组建的导入。

Next.js使用的是基于文件系统的路由，只要在文件夹中有一个`page.tsx`文件就可以在路由中找到该页面。
